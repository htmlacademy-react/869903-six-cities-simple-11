export type UserData = {
  id: number;
  email: string;
  token: string;
};
export type ResponseUserData = {
  id: number;
  email: string;
  name: string;
  avatarUrl: string;
  isPro: boolean;
  token: string;
}
